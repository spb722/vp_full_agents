# VP Agent Context

Follow the project rules in `AGENTS.md`.

The two hard invariants are:

1. Never write VP condition syntax outside `render_condition`.
2. Treat routing, rendering, validation, and metadata lookup as deterministic
   tool outputs.

